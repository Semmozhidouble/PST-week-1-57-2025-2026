# Portfolio — semmozhiyan

Simple static portfolio created with HTML & CSS.

How to preview
1. Open `index.html` in your browser (double-click or right-click → Open with → browser).

Replace the profile image
- Put your photo at `images/profile.jpg` or replace `images/profile.svg` with your real photo and keep the filename the same.

Want changes?
- Tell me if you want different colors, fonts, or additional sections (projects details, resume, blog, etc.).